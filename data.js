// data.js
// Centralise les données du jeu (coûts, multiplicateurs de base, données des succès, etc.).

const eleveBaseCost = 10;
const classeBaseCost = 100;
const professeurBaseCost = 1000;
const imageBaseCost = 50;

const classeMultiplier = 10;
const professeurMultiplier = 100;
const imageMultiplier = 50;

const prestigeBaseCost = 1e6;
const ascensionBaseCost = 1e10;

// ... (Autres données du jeu)

// Exemple de données pour les succès (à compléter)
const achievementsData = [
    { id: "first-click", description: "Effectuer votre premier clic." },
    { id: "1000-clicks", description: "Effectuer 1,000 clics." },
    // ...
];

// Exemple de données pour les quêtes (à compléter)
const questData = [
  {id: "first-quest", description: "Avoir 100 élèves", progress: 0, goal: 100, reward: 1000},
  //...
]
